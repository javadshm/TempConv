apiVersion: v1
kind: Service
metadata:
  name: tempconv-frontend
  namespace: tempconv
  labels:
    app: tempconv-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: tempconv-frontend
